/*const TaskEditor = ({index}) => {

    const handleSave = () => {
        const updatedTask = {...task, name: task.name}
        updateTask(updatedTask);
    }

    return(
        <div>
            <input
            value={task.name}>

            </input>
            <button onClick={() => handleSave(index)}>Salvar</button>
        </div>
    )
}

export default TaskEditor*/